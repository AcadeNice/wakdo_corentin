meta {
  name: Relire apres suppression (404 attendu)
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/admin/api/products/{{created_product_id}}
}

docs {
Preuve de nettoyage : suppression dure, la ressource n'existe plus.
}

tests {
  expect(res.getStatus()).to.equal(404);
}
