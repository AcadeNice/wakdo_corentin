meta {
  name: Relire le role apres desactivation (is_active=false)
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/admin/api/roles/{{created_role_id}}
}

docs {
Preuve de nettoyage.
}

tests {
  const body = res.getBody();
  expect(res.getStatus()).to.equal(200);
  expect(body.data.is_active).to.equal(false);
}
