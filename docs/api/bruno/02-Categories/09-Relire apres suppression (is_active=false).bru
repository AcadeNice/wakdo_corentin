meta {
  name: Relire apres suppression (is_active=false)
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/admin/api/categories/{{created_category_id}}
}

docs {
Preuve de nettoyage : la categorie existe toujours (pas de suppression dure) mais est desactivee.
}

tests {
  const body = res.getBody();
  expect(res.getStatus()).to.equal(200);
  expect(body.data.is_active).to.equal(false);
}
