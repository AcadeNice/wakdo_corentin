meta {
  name: Autorise : lister les commandes (order.read)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/admin/api/orders
}

docs {
La cuisine a order.read (seed, KDS) : 200 attendu.
}

tests {
  expect(res.getStatus()).to.equal(200);
}
